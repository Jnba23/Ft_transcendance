tree 3b2ef47672359ae262877a689778e7e3779bff10
parent c26c5a2f3b7d49b9f79c5e424bda014e89f972e7
author jnba23 <sayadabdelwadoud@gmail.com> 1771395584 +0000
committer jnba23 <sayadabdelwadoud@gmail.com> 1771398486 +0000

feat(backend): implement real-time RPS game with matchmaking and websockets

     Socket.io Infrastructure:
     - Set up Socket.io server in core/sockets/socketServer.ts
     - Integrate Socket.io with Express HTTP server
     - Configure CORS for socket connections
     - Create namespace architecture (/matchmaking, /rps, /pong, /chat)
     - Register all socket handlers in centralized initialization

     Matchmaking System:
     - Implement MatchmakingService singleton for queue management
     - Add random matchmaking algorithm (matches players by gameType)
     - Create matchmaking socket handlers (join-queue, leave-queue, disconnect)
     - Store queue entries with userId, username, gameType, socketId
     - Generate unique gameId on successful match
     - Emit 'match-found' event to both players with game details

     Game Session Management:
     - Create SessionManager to track active game sessions
     - Store MatchResult objects keyed by gameId
     - Provide session lifecycle methods (create, get, delete)
     - Enable cross-namespace data sharing between matchmaking and game handlers

     RPS Game Logic:
     - Implement RpsGameManager singleton for game state management
     - Add rock-paper-scissors winner determination logic
     - Track per-round scores and game progression (best of 3)
     - Manage player states (choices, scores, connection status)
     - Implement timer management system for game events

     Timer & Reconnection System:
     - Add 5-second auto-choice timer for inactive players
     - Add 5-second reconnection grace period on disconnect
     - Implement timer lifecycle management (start, clear, cleanup)
     - Add connection state tracking (isConnected, socketId)
     - Handle forfeit on reconnection timeout

     Game Persistence:
     - Create gamePersistence service for database operations
     - Save completed games to games table with scores and winner
     - Update user statistics (RPS_wins, RPS_losses) on game completion
     - Implement transaction-based updates for data consistency
     - Add getUserGameHistory and getUserStats query functions

     RPS Socket Events:
     - join-game: Player joins game room with validation
     - make-choice: Record player choice (rock/paper/scissors)
     - game-start: Initialize round with countdown
     - choice-recorded: Confirm choice received
     - round-results: Reveal both choices and round winner
     - new-round: Start next round with fresh choices
     - game-over: End game with final scores and winner
     - auto-choice-made: Notify when player times out
     - opponent-disconnected: Alert on player disconnect
     - disconnect: Handle disconnection with grace period

     Types & Interfaces:
     - Define GameType ('rps' | 'pong')
     - Define Choice ('rock' | 'paper' | 'scissors')
     - Define GamePhase ('waiting' | 'choosing' | 'revealing' | 'game-over')
     - Create QueueEntry, MatchResult interfaces for matchmaking
     - Create PlayerState, RpsGameState interfaces for game state
     - Add timer references to game state
