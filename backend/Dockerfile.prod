FROM node:20-alpine
RUN apk add --no-cache python3 make g++
RUN npm install -g tsx
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev
COPY . .
RUN mkdir -p /app/data
EXPOSE 3000
CMD [ "tsx", "src/server.ts" ]
